/* JAVASCRIPT GENERATORS */

/* Generators are special kinds of functions which can
 * pause itself, allow other code to run in the meantime,
 * then resume afterwards or later. Main components: it can
 * wait -> lets other code "in the queue to run"
 */
